  <h3 class="mt-4">Cập nhật cơ sở bán thuốc thú y</h3>

  <div class="mt-4">
    <form method="POST" action="/vetPharmacy/{{vetPharmacy.id}}?_method=PUT">
      <div class="form-group">
        <label for="name">Tên cơ sở</label>
        <input type="text" class="form-control" value="{{vetPharmacy.name}}" id="name" name="name">
      </div>
      <div class="form-group">
        <label for="location">Địa chỉ</label>
        <input type="text" class="form-control" value="{{vetPharmacy.location}}" id="location" name="location">
      </div>
      <div class="form-group">
        <label for="contact_number">Số điện thoại</label>
        <input type="text" class="form-control" value="{{vetPharmacy.contact_number}}" id="contact_number" name="contact_number">
      </div>
      <div class="form-group">
        <label for="opening_hours">Giờ mở cửa</label>
        <input type="text" class="form-control" value="{{vetPharmacy.opening_hours}}" id="opening_hours" name="opening_hours">
      </div>
      <button type="submit" class="btn btn-primary mt-4">Lưu</button>
    </form>
  </div>
</div>